const app = require("../app");
const debug = require("debug")("authpassport:server");
const http = require("http");
require("dotenv").config();

const port = process.env.PORT || 5500;
app.set("port", port);

const server = http.createServer(app);

server.listen(port);